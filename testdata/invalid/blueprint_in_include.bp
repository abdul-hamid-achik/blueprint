# This file is meant to be included, but it has a blueprint block.
# The checker should reject blueprint blocks in included files.
blueprint "included" {
  version "0.1.0"
  port    3000
  runtime node
}

model item {
  id   uuid   primary
  name string required
}
